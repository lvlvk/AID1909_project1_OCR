view包含显示界面和与用户交互的相关功能
	用户的输入：
		人脸识别请求
		车牌识别请求
		信息录入(后面有时间再考虑)
	对用户的相应：
		人脸识别：让用户选择一张照片，调用相应识别模块提供的API接口并得到结果。将这个结果传给服务端，并等待服务端的结果。
		车牌识别：过程与人脸识别相同。
	给用户的结果：
		在发送给服务端、等待服务端处理期间显示相应状态信息(如“发送中。。。”、“处理中。。。”)
		等到服务端发送来的结果，根据结果显示不同信息(如“通过”、“查无此人”、“查无此车”)
